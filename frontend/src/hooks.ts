import {useEffect,useState} from 'react'
export function useLoad<T>(loader:()=>Promise<T>,deps:unknown[]=[]){const [data,setData]=useState<T|null>(null);const [error,setError]=useState('');const [loading,setLoading]=useState(true);useEffect(()=>{let active=true;setLoading(true);setError('');loader().then(v=>{if(active)setData(v)}).catch(e=>{if(active)setError(e instanceof Error?e.message:'Unable to load this view.')}).finally(()=>{if(active)setLoading(false)});return()=>{active=false}},deps);return{data,error,loading,setData}}
